# Version

**Current version: 2.0.0** (2026-07-18)

Package: `airflow-provider-pentaho`
Python module: `airflow_pentaho`
Supports: Apache Airflow >= 2.7 (including 3.x), Python 3.9–3.12

The single source of truth for the version number is
`airflow_pentaho/__init__.py` (`__version__`) and `pyproject.toml`
(`project.version`). Keep both in sync and update this file and
CHANGELOG.md on every release.
